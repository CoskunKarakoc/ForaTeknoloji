@model ForaTeknoloji.PresentationLayer.Models.AlarmListViewModel
@{
    ViewBag.Title = "Alarmlar";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="row">
    <div class="col-md-12">
        <div class="card shadow-lg mt-5">
            <div class="card-header-tabs mt-1">
                <h3 class="text-muted text-center"></h3>
            </div>
            <div class="card-body">
                <table class="">
                    <thead class="table-info">
                        <tr>
                            <td>Alarm No</td>
                            <td>Alarm Adi</td>
                            <td>Alarm Tipi Adi</td>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model.Alarmlar)
                        {
                            <tr>
                                <td>@item.Alarm_No</td>
                                <td>@item.Alarm_Adi</td>
                                <td>@item.Adi</td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
